# 在线选择临界点实验日志

## 开工与边界

- 已读取上一轮学习实验和直接执行器完整报告、说明、冻结模型及相关源码；无适用 AGENTS.md。
- 从包含历史未提交文件的工作区创建 `experiment/selection-break-even`。创建分支首次被 .git 沙箱拦截，经单独授权成功；没有绕过权限。
- 记录 868 个既有非忽略文件的 SHA-256。Lean 相关后台进程为 VS Code 服务器／文件 worker，无本实验路径写入任务；不改其他项目或 worktree。
- 本轮仅新增 BudgetSelection 及本实验目录。候选、内核、规划器和历史模型不变。无 commit/push。
- 使用数据验证、统计分析、可视化、测试与文档技能：保留原始重复、失败／未执行项，采用逐输入配对和形态分组，不以重复次数充当独立输入。

## 预定实现选择（尚未测新性能）

- A 使用上一轮 Worker 与最终冻结模型原样执行，先 pilot 确定资源／长度范围，再冻结正式协议。
- B 仅比较 length / 8 / 16 / legacy 四种预算。8/16 各探针只做一次相邻键 ≤ 比较，特征为 n、探针数、下降率；其他槽显式为零。旧六维接口用于复用轻量训练器，并非伪造缺失特征。
- 先 List.length，再按单调探针位置向前推进链表；不从头独立索引每个探针。两次有界向前遍历和小列表分配计入在线时间，不称 O(1)。不复用 runs。
- B 模型与人工规则读取相同特征向量；规则和模型搜索空间预先有限。B 的训练／验证／确认与 A 和旧实验均隔离。
- 结束条件是完成预算内两阶段、图表与验收，负结果不触发新一轮调参。
